using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace AltConsole.Common
{
    public static class Extensions
    {
        //public static char[] Concat(this char[] thisArray, char[] other)
        //{
        //    var newArray = char[thisArray.Length + other.Length];

        //    Array.Copy(thisArray, newArray, thisArray.Length);
        //    Array.Copy(other, 0, newArray, thisArray.Length, other.Length);

        //    return newArray;
        //}
    }
}
